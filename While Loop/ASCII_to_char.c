#include <stdio.h>
int main()
{
    int i, ch;
    // printf("Enter a num : ");
    // scanf("%c", &ch);
    i = 0;
    while (i <= 255)
    {
        printf("ASCCi value of cher %c = %d\n", i, i);
        i++;
    }

    return 0;
}