#include <stdio.h>
int main()
{
    int i, sum = 1, n = 0, num;
    printf("Enter a num : ");
    scanf("%d", &num);
    for (i = 0; i < num; i++)
    {
        sum += n;
        printf("%d ", sum);
        n++;
    }
    return 0;
}